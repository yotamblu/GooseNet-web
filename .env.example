# Public site URL (used for canonical, Open Graph, sitemap, and JSON-LD).
# Set in .env.local for local overrides.
NEXT_PUBLIC_SITE_URL=https://goosenet.space

# API base (optional override; default is https://api.goosenet.space)
# NEXT_PUBLIC_API_BASE_URL=https://api.goosenet.space

# Google OAuth (required for sign-in)
# NEXT_PUBLIC_GOOGLE_CLIENT_ID=
